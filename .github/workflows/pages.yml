name: Deploy Pages
on:
  push:
    branches: [ "web" ]
  pull_request:
    branches: [ "web" ]
  workflow_dispatch:
jobs:
  build:
    uses: doriannaujokat/github-workflows/.github/workflows/pages-typescript.yml@main
